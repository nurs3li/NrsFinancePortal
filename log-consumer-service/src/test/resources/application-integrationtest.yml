spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
  data:
    redis:
      timeout: 3s

management:
  tracing:
    sampling:
      probability: 0.0
  otlp:
    tracing:
      endpoint: http://localhost:65535/v1/traces

logging:
  level:
    com.nurseli.logconsumer: DEBUG
