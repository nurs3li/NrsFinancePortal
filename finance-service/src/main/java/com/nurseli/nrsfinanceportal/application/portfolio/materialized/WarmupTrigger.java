package com.nurseli.nrsfinanceportal.application.portfolio.materialized;

/** Materialized warmup tetikleyicisi — ADD için artımlı timeseries birleştirmesi kullanılabilir. */
public enum WarmupTrigger {
    FULL,
    ADD
}
